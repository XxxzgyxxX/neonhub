
-- Neon Hub V1 | Complete Blox Fruits Script
-- Created By XzgyX
-- Compatible with KNRL Mobile and other executors

-- Check if running in Roblox environment
if not game then
    print("Este script deve ser executado dentro do Roblox!")
    return
end

-- Wait for game to load
repeat task.wait() until game:IsLoaded()

-- Services
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local Workspace = game:GetService("Workspace")
local VirtualUser = game:GetService("VirtualUser")
local TeleportService = game:GetService("TeleportService")
local HttpService = game:GetService("HttpService")
local Lighting = game:GetService("Lighting")
local StarterGui = game:GetService("StarterGui")
local CoreGui = game:GetService("CoreGui")

-- Variables
local Player = Players.LocalPlayer
local Character = Player.Character or Player.CharacterAdded:Wait()
local Humanoid = Character:WaitForChild("Humanoid")
local RootPart = Character:WaitForChild("HumanoidRootPart")

-- World Detection
local World1 = game.PlaceId == 2753915549
local World2 = game.PlaceId == 4442272183  
local World3 = game.PlaceId == 7449423635

-- Mobile executor compatibility check
local isMobile = UserInputService.TouchEnabled

-- Enhanced Anti-Detection for Mobile
local function setupAntiDetection()
    local success = pcall(function()
        -- Basic anti-detection for mobile
        if getrawmetatable then
            local mt = getrawmetatable(game)
            if mt then
                local old = mt.__namecall
                if setreadonly then
                    setreadonly(mt, false)
                    mt.__namecall = function(self, ...)
                        local method = getnamecallmethod()
                        if method == "FireServer" or method == "InvokeServer" then
                            local name = tostring(self)
                            if name:find("Kick") or name:find("Ban") or name:find("Log") then
                                return
                            end
                        end
                        return old(self, ...)
                    end
                    setreadonly(mt, true)
                end
            end
        end
    end)

    if not success then
        print("⚠️ Aviso: Algumas proteções podem não estar ativas")
    end
end

setupAntiDetection()

-- Settings Storage
local Settings = {
    AutoFarmLevel = false,
    AutoFarmNearest = false,
    AutoFarmBone = false,
    AutoFarmElite = false,
    FastAttack = false,
    BringMobs = false,
    AutoClick = false,
    AutoKen = false,
    AutoBuso = false,
    AutoStats = false,
    SelectedStats = "Melee",
    FarmDistance = 20,
    AttackDelay = 0.1,
    WalkSpeed = 16,
    JumpPower = 50,
    NoClip = false,
    AutoRejoin = false,
    AntiAFK = true
}

-- Variables for Quest System
local Ms, NaemQuest, LevelQuest, NameMon, CFrameQuest, CFrameMon

-- Mobile-Optimized UI Creation
local function CreateMobileUI()
    -- Remove existing UI if present
    if CoreGui:FindFirstChild("NeonHubMobileUI") then
        CoreGui:FindFirstChild("NeonHubMobileUI"):Destroy()
    end

    -- Main ScreenGui
    local ScreenGui = Instance.new("ScreenGui")
    ScreenGui.Name = "NeonHubMobileUI"
    ScreenGui.Parent = CoreGui
    ScreenGui.ResetOnSpawn = false

    -- Mobile-optimized dimensions
    local screenSize = workspace.CurrentCamera.ViewportSize
    local isMobileScreen = screenSize.X < 800 or screenSize.Y < 600

    local containerWidth = isMobileScreen and math.min(screenSize.X * 0.95, 400) or 500
    local containerHeight = isMobileScreen and math.min(screenSize.Y * 0.8, 600) or 600

    -- Main Container
    local MainContainer = Instance.new("Frame")
    MainContainer.Name = "MainContainer"
    MainContainer.Parent = ScreenGui
    MainContainer.Size = UDim2.new(0, containerWidth, 0, containerHeight)
    MainContainer.Position = UDim2.new(0.5, -containerWidth/2, 0.5, -containerHeight/2)
    MainContainer.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
    MainContainer.BorderSizePixel = 0
    MainContainer.Active = true
    MainContainer.Draggable = not isMobile

    -- Rounded corners
    local MainCorner = Instance.new("UICorner")
    MainCorner.CornerRadius = UDim.new(0, 12)
    MainCorner.Parent = MainContainer

    -- Top Bar
    local TopBar = Instance.new("Frame")
    TopBar.Name = "TopBar"
    TopBar.Parent = MainContainer
    TopBar.Size = UDim2.new(1, 0, 0, 50)
    TopBar.BackgroundColor3 = Color3.fromRGB(0, 162, 255)
    TopBar.BorderSizePixel = 0

    local TopBarCorner = Instance.new("UICorner")
    TopBarCorner.CornerRadius = UDim.new(0, 12)
    TopBarCorner.Parent = TopBar

    -- Title
    local TitleLabel = Instance.new("TextLabel")
    TitleLabel.Parent = TopBar
    TitleLabel.Size = UDim2.new(1, -100, 1, 0)
    TitleLabel.Position = UDim2.new(0, 10, 0, 0)
    TitleLabel.BackgroundTransparency = 1
    TitleLabel.Text = "🌟 NEON HUB V1"
    TitleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    TitleLabel.TextScaled = true
    TitleLabel.Font = Enum.Font.GothamBold

    -- Close Button
    local CloseButton = Instance.new("TextButton")
    CloseButton.Parent = TopBar
    CloseButton.Size = UDim2.new(0, 40, 0, 40)
    CloseButton.Position = UDim2.new(1, -45, 0, 5)
    CloseButton.BackgroundColor3 = Color3.fromRGB(255, 80, 80)
    CloseButton.Text = "X"
    CloseButton.TextColor3 = Color3.fromRGB(255, 255, 255)
    CloseButton.TextScaled = true
    CloseButton.Font = Enum.Font.GothamBold

    local CloseCorner = Instance.new("UICorner")
    CloseCorner.CornerRadius = UDim.new(0, 8)
    CloseCorner.Parent = CloseButton

    -- Scrolling Content
    local ScrollFrame = Instance.new("ScrollingFrame")
    ScrollFrame.Parent = MainContainer
    ScrollFrame.Size = UDim2.new(1, -20, 1, -70)
    ScrollFrame.Position = UDim2.new(0, 10, 0, 60)
    ScrollFrame.BackgroundTransparency = 1
    ScrollFrame.ScrollBarThickness = 8
    ScrollFrame.CanvasSize = UDim2.new(0, 0, 0, 0)

    local ContentLayout = Instance.new("UIListLayout")
    ContentLayout.Parent = ScrollFrame
    ContentLayout.Padding = UDim.new(0, 10)
    ContentLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center

    -- Auto-resize canvas
    ContentLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
        ScrollFrame.CanvasSize = UDim2.new(0, 0, 0, ContentLayout.AbsoluteContentSize.Y + 20)
    end)

    -- Mobile Toggle Function
    local function CreateMobileToggle(name, description, callback)
        local ToggleFrame = Instance.new("Frame")
        ToggleFrame.Parent = ScrollFrame
        ToggleFrame.Size = UDim2.new(1, -10, 0, 70)
        ToggleFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
        ToggleFrame.BorderSizePixel = 0

        local ToggleCorner = Instance.new("UICorner")
        ToggleCorner.CornerRadius = UDim.new(0, 8)
        ToggleCorner.Parent = ToggleFrame

        local ToggleLabel = Instance.new("TextLabel")
        ToggleLabel.Parent = ToggleFrame
        ToggleLabel.Size = UDim2.new(1, -80, 0, 35)
        ToggleLabel.Position = UDim2.new(0, 10, 0, 5)
        ToggleLabel.BackgroundTransparency = 1
        ToggleLabel.Text = name
        ToggleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
        ToggleLabel.TextXAlignment = Enum.TextXAlignment.Left
        ToggleLabel.TextScaled = true
        ToggleLabel.Font = Enum.Font.GothamBold

        local DescLabel = Instance.new("TextLabel")
        DescLabel.Parent = ToggleFrame
        DescLabel.Size = UDim2.new(1, -80, 0, 25)
        DescLabel.Position = UDim2.new(0, 10, 0, 40)
        DescLabel.BackgroundTransparency = 1
        DescLabel.Text = description
        DescLabel.TextColor3 = Color3.fromRGB(180, 180, 180)
        DescLabel.TextXAlignment = Enum.TextXAlignment.Left
        DescLabel.TextScaled = true
        DescLabel.Font = Enum.Font.Gotham

        -- Mobile-friendly toggle button
        local ToggleButton = Instance.new("TextButton")
        ToggleButton.Parent = ToggleFrame
        ToggleButton.Size = UDim2.new(0, 60, 0, 30)
        ToggleButton.Position = UDim2.new(1, -70, 0, 20)
        ToggleButton.BackgroundColor3 = Color3.fromRGB(60, 60, 70)
        ToggleButton.Text = "OFF"
        ToggleButton.TextColor3 = Color3.fromRGB(255, 255, 255)
        ToggleButton.TextScaled = true
        ToggleButton.Font = Enum.Font.GothamBold

        local ButtonCorner = Instance.new("UICorner")
        ButtonCorner.CornerRadius = UDim.new(0, 6)
        ButtonCorner.Parent = ToggleButton

        local toggled = false
        ToggleButton.MouseButton1Click:Connect(function()
            toggled = not toggled

            if toggled then
                ToggleButton.BackgroundColor3 = Color3.fromRGB(0, 255, 100)
                ToggleButton.Text = "ON"
                ToggleFrame.BackgroundColor3 = Color3.fromRGB(35, 45, 35)
            else
                ToggleButton.BackgroundColor3 = Color3.fromRGB(60, 60, 70)
                ToggleButton.Text = "OFF"
                ToggleFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
            end

            callback(toggled)
        end)

        return ToggleFrame
    end

    -- Create Sections
    local function CreateSection(title)
        local SectionLabel = Instance.new("TextLabel")
        SectionLabel.Parent = ScrollFrame
        SectionLabel.Size = UDim2.new(1, -10, 0, 30)
        SectionLabel.BackgroundTransparency = 1
        SectionLabel.Text = "📋 " .. title
        SectionLabel.TextColor3 = Color3.fromRGB(0, 162, 255)
        SectionLabel.TextXAlignment = Enum.TextXAlignment.Left
        SectionLabel.TextScaled = true
        SectionLabel.Font = Enum.Font.GothamBold

        return SectionLabel
    end

    -- Farm Section
    CreateSection("FARM")
    CreateMobileToggle("Auto Farm Level", "Farm automaticamente seu level", function(value)
        Settings.AutoFarmLevel = value
        if value then AutoFarmLevel() end
    end)

    CreateMobileToggle("Auto Farm Bone", "Farm bones no 3º mundo", function(value)
        Settings.AutoFarmBone = value
        if value then AutoBone() end
    end)

    CreateMobileToggle("Auto Farm Elite", "Caça elites automaticamente", function(value)
        Settings.AutoFarmElite = value
        if value then AutoElite() end
    end)

    CreateMobileToggle("Bring Mobs", "Puxa inimigos para você", function(value)
        Settings.BringMobs = value
    end)

    -- Combat Section
    CreateSection("COMBAT")
    CreateMobileToggle("Fast Attack", "Ataque mais rápido", function(value)
        Settings.FastAttack = value
    end)

    CreateMobileToggle("Auto Buso Haki", "Ativa Buso automaticamente", function(value)
        Settings.AutoBuso = value
    end)

    CreateMobileToggle("Auto Ken Haki", "Ativa Ken automaticamente", function(value)
        Settings.AutoKen = value
    end)

    -- Player Section
    CreateSection("PLAYER")
    CreateMobileToggle("No Clip", "Atravessa paredes", function(value)
        Settings.NoClip = value
        if value then
            task.spawn(function()
                while Settings.NoClip do
                    task.wait(0.1)
                    if Character then
                        for _, part in pairs(Character:GetChildren()) do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                end
            end)
        end
    end)

    CreateMobileToggle("Auto Stats", "Adiciona pontos no Melee", function(value)
        Settings.AutoStats = value
        Settings.SelectedStats = "Melee"
        if value then AutoStats() end
    end)

    -- Misc Section
    CreateSection("MISC")
    CreateMobileToggle("Anti AFK", "Previne kick por inatividade", function(value)
        Settings.AntiAFK = value
    end)

    -- Close button functionality
    CloseButton.MouseButton1Click:Connect(function()
        ScreenGui:Destroy()
    end)

    -- Mobile notification
    task.spawn(function()
        task.wait(1)
        pcall(function()
            StarterGui:SetCore("SendNotification", {
                Title = "🌟 Neon Hub V1",
                Text = "Carregado! Compatível com KNRL Mobile",
                Duration = 5
            })
        end)
    end)
end

-- Anti AFK System
if Settings.AntiAFK then
    Player.Idled:Connect(function()
        VirtualUser:Button2Down(Vector2.new(0,0),workspace.CurrentCamera.CFrame)
        task.wait(1)
        VirtualUser:Button2Up(Vector2.new(0,0),workspace.CurrentCamera.CFrame)
    end)
end

-- Enhanced Fast Attack for Mobile
local AttackFunction
local function setupFastAttack()
    local success, result = pcall(function()
        if not Player.PlayerScripts:FindFirstChild("CombatFramework") then
            return function() end
        end

        local CombatFramework = require(Player.PlayerScripts:WaitForChild("CombatFramework"))
        local CombatFrameworkR = getupvalues(CombatFramework)[2]
        local RigController = require(Player.PlayerScripts.CombatFramework.RigController)

        function CurrentWeapon()
            local ac = CombatFrameworkR.activeController
            if not ac then return nil end
            local ret = ac.blades[1]
            if not ret then return Player.Character:FindFirstChildOfClass("Tool") and Player.Character:FindFirstChildOfClass("Tool").Name end
            pcall(function()
                while ret.Parent~=Player.Character do ret=ret.Parent end
            end)
            return ret
        end

        return function()
            if not Settings.FastAttack then return end
            local ac = CombatFrameworkR.activeController
            if ac and ac.equipped then
                for indexincrement = 1, 1 do
                    local bladehit = require(Player.PlayerScripts.CombatFramework.RigLib).getBladeHits(Player.Character,{Player.Character.HumanoidRootPart},60)
                    local cac = {}
                    local hash = {}
                    for k, v in pairs(bladehit) do
                        if v.Parent:FindFirstChild("HumanoidRootPart") and not hash[v.Parent] then
                            table.insert(cac, v.Parent.HumanoidRootPart)
                            hash[v.Parent] = true
                        end
                    end
                    bladehit = cac
                    if #bladehit > 0 then
                        local u8 = debug.getupvalue(ac.attack, 5)
                        local u9 = debug.getupvalue(ac.attack, 6)
                        local u7 = debug.getupvalue(ac.attack, 4)
                        local u10 = debug.getupvalue(ac.attack, 7)
                        local u12 = (u8 * 798405 + u7 * 727595) % u9
                        local u13 = u7 * 798405
                        u12 = (u12 * u9 + u13) % 1099511627776
                        u8 = math.floor(u12 / u9)
                        u7 = u12 - u8 * u9
                        ac.activeController.timeToNextAttack = 0
                        ac.activeController.attacking = false
                        ac.activeController.timeToNextBlock = 0
                        ac.activeController.blocking = false
                        ac.activeController.hitboxMagnitude = 60
                        game:GetService("ReplicatedStorage").RigControllerEvent:FireServer("weaponChange",tostring(CurrentWeapon()))
                        game.ReplicatedStorage.Remotes.Validator:FireServer(math.floor(u12 / 1099511627776 * 16777215), u10)
                        game:GetService("ReplicatedStorage").RigControllerEvent:FireServer("hit", bladehit, indexincrement, "")
                    end
                end
            end
        end
    end)

    if success then
        AttackFunction = result
    else
        AttackFunction = function() end
        print("⚠️ Fast Attack não disponível neste executor")
    end
end

setupFastAttack()

-- Auto Click System
function AutoClick()
    if Settings.AutoClick then
        pcall(function()
            game:GetService("VirtualUser"):CaptureController()
            game:GetService("VirtualUser"):Button1Down(Vector2.new(1280, 672))
        end)
    end
end

-- Quest System
function GetQuest()
    local MyLevel = Player.Data.Level.Value

    if World1 then
        if MyLevel >= 1 and MyLevel <= 9 then
            Ms = "Bandit"
            NaemQuest = "BanditQuest1"
            LevelQuest = 1
            NameMon = "Bandit"
            CFrameQuest = CFrame.new(1059.37195, 15.4495068, 1550.4231)
            CFrameMon = CFrame.new(1045.962646484375, 27.00250816345215, 1560.8203125)
        elseif MyLevel >= 10 and MyLevel <= 14 then
            Ms = "Monkey"
            NaemQuest = "JungleQuest"
            LevelQuest = 1
            NameMon = "Monkey"
            CFrameQuest = CFrame.new(-1598.08911, 35.5501175, 153.377838)
            CFrameMon = CFrame.new(-1448.51806640625, 67.85301208496094, 11.46579647064209)
        elseif MyLevel >= 15 and MyLevel <= 29 then
            Ms = "Gorilla"
            NaemQuest = "JungleQuest"
            LevelQuest = 2
            NameMon = "Gorilla"
            CFrameQuest = CFrame.new(-1598.08911, 35.5501175, 153.377838)
            CFrameMon = CFrame.new(-1129.8836669921875, 40.46354675292969, -525.4237060546875)
        elseif MyLevel >= 60 and MyLevel <= 74 then
            Ms = "Desert Bandit"
            NaemQuest = "DesertQuest"
            LevelQuest = 1
            NameMon = "Desert Bandit"
            CFrameQuest = CFrame.new(894.488647, 5.14000702, 4392.43359)
            CFrameMon = CFrame.new(924.7998046875, 6.44867467880249, 4481.5859375)
        else
            Ms = "Bandit"
            NaemQuest = "BanditQuest1"
            LevelQuest = 1
            NameMon = "Bandit"
            CFrameQuest = CFrame.new(1059.37195, 15.4495068, 1550.4231)
            CFrameMon = CFrame.new(1045.962646484375, 27.00250816345215, 1560.8203125)
        end
    elseif World2 then
        if MyLevel >= 700 and MyLevel <= 724 then
            Ms = "Raider"
            NaemQuest = "Area1Quest"
            LevelQuest = 1
            NameMon = "Raider"
            CFrameQuest = CFrame.new(-429.543518, 71.7699966, 1836.18188)
            CFrameMon = CFrame.new(-728.3267211914062, 52.779319763183594, 2345.7705078125)
        else
            Ms = "Raider"
            NaemQuest = "Area1Quest"
            LevelQuest = 1
            NameMon = "Raider"
            CFrameQuest = CFrame.new(-429.543518, 71.7699966, 1836.18188)
            CFrameMon = CFrame.new(-728.3267211914062, 52.779319763183594, 2345.7705078125)
        end
    elseif World3 then
        if MyLevel >= 1500 and MyLevel <= 1524 then
            Ms = "Pirate Millionaire"
            NaemQuest = "PiratePortQuest"
            LevelQuest = 1
            NameMon = "Pirate Millionaire"
            CFrameQuest = CFrame.new(-290.074677, 42.9034653, 5581.58984)
            CFrameMon = CFrame.new(-245.9963836669922, 47.30615234375, 5584.1005859375)
        else
            Ms = "Pirate Millionaire"
            NaemQuest = "PiratePortQuest"
            LevelQuest = 1
            NameMon = "Pirate Millionaire"
            CFrameQuest = CFrame.new(-290.074677, 42.9034653, 5581.58984)
            CFrameMon = CFrame.new(-245.9963836669922, 47.30615234375, 5584.1005859375)
        end
    end
end

-- Enhanced Tween Function
function topos(Pos)
    if not Pos then return end
    local Distance = (Pos.Position - Player.Character.HumanoidRootPart.Position).Magnitude
    local Speed = 300

    if Distance < 25 then
        Speed = 5000
    elseif Distance < 50 then
        Speed = 2000
    elseif Distance < 150 then
        Speed = 600
    elseif Distance < 300 then
        Speed = 300
    end

    local tween = TweenService:Create(
        Player.Character.HumanoidRootPart,
        TweenInfo.new(Distance/Speed, Enum.EasingStyle.Linear),
        {CFrame = Pos}
    )
    tween:Play()
    return tween
end

-- Auto Farm Level Function
function AutoFarmLevel()
    task.spawn(function()
        while Settings.AutoFarmLevel do
            task.wait(0.1)
            pcall(function()
                GetQuest()

                if not Player.PlayerGui.Main.Quest.Visible or (Player.PlayerGui.Main.Quest.Container.QuestTitle.Title.Text ~= NameMon) then
                    ReplicatedStorage.Remotes.CommF_:InvokeServer("StartQuest", NaemQuest, LevelQuest)
                end

                for i,v in pairs(Workspace.Enemies:GetChildren()) do
                    if v.Name == Ms and v:FindFirstChild("Humanoid") and v:FindFirstChild("HumanoidRootPart") and v.Humanoid.Health > 0 then
                        repeat task.wait()
                            if Settings.BringMobs then
                                v.HumanoidRootPart.CanCollide = false
                                v.Humanoid.WalkSpeed = 0
                                v.HumanoidRootPart.Size = Vector3.new(80,80,80)
                                v.HumanoidRootPart.CFrame = Player.Character.HumanoidRootPart.CFrame * CFrame.new(0, 0, -3)
                            end

                            topos(v.HumanoidRootPart.CFrame * CFrame.new(0,Settings.FarmDistance,0))

                            if Settings.AutoBuso then
                                if not Player.Character:FindFirstChild("HasBuso") then
                                    ReplicatedStorage.Remotes.CommF_:InvokeServer("Buso")
                                end
                            end

                            if sethiddenproperty then
                                sethiddenproperty(Player, "SimulationRadius", math.huge)
                            end
                        until not Settings.AutoFarmLevel or v.Humanoid.Health <= 0 or not v.Parent
                    end
                end
            end)
        end
    end)
end

-- Auto Farm Bone Function
function AutoBone()
    task.spawn(function()
        while Settings.AutoFarmBone do
            task.wait(0.1)
            pcall(function()
                if World3 then
                    for i,v in pairs(Workspace.Enemies:GetChildren()) do
                        if (v.Name == "Reborn Skeleton" or v.Name == "Living Zombie" or v.Name == "Demonic Soul" or v.Name == "Posessed Mummy") and v:FindFirstChild("Humanoid") and v:FindFirstChild("HumanoidRootPart") and v.Humanoid.Health > 0 then
                            repeat task.wait()
                                if Settings.BringMobs then
                                    v.HumanoidRootPart.CanCollide = false
                                    v.Humanoid.WalkSpeed = 0
                                    v.HumanoidRootPart.Size = Vector3.new(80,80,80)
                                    v.HumanoidRootPart.CFrame = Player.Character.HumanoidRootPart.CFrame * CFrame.new(0, 0, -3)
                                end

                                topos(v.HumanoidRootPart.CFrame * CFrame.new(0,Settings.FarmDistance,0))

                                if Settings.AutoBuso then
                                    if not Player.Character:FindFirstChild("HasBuso") then
                                        ReplicatedStorage.Remotes.CommF_:InvokeServer("Buso")
                                    end
                                end

                                if sethiddenproperty then
                                    sethiddenproperty(Player, "SimulationRadius", math.huge)
                                end
                            until not Settings.AutoFarmBone or v.Humanoid.Health <= 0 or not v.Parent
                        end
                    end
                end
            end)
        end
    end)
end

-- Auto Elite Hunter Function
function AutoElite()
    task.spawn(function()
        while Settings.AutoFarmElite do
            task.wait(0.1)
            pcall(function()
                for i,v in pairs(Workspace.Enemies:GetChildren()) do
                    if (v.Name == "Diablo" or v.Name == "Deandre" or v.Name == "Urban") and v:FindFirstChild("Humanoid") and v:FindFirstChild("HumanoidRootPart") and v.Humanoid.Health > 0 then
                        repeat task.wait()
                            if Settings.BringMobs then
                                v.HumanoidRootPart.CanCollide = false
                                v.Humanoid.WalkSpeed = 0
                                v.HumanoidRootPart.Size = Vector3.new(80,80,80)
                                v.HumanoidRootPart.CFrame = Player.Character.HumanoidRootPart.CFrame * CFrame.new(0, 0, -3)
                            end

                            topos(v.HumanoidRootPart.CFrame * CFrame.new(0,Settings.FarmDistance,0))

                            if Settings.AutoBuso then
                                if not Player.Character:FindFirstChild("HasBuso") then
                                    ReplicatedStorage.Remotes.CommF_:InvokeServer("Buso")
                                end
                            end

                            if sethiddenproperty then
                                sethiddenproperty(Player, "SimulationRadius", math.huge)
                            end
                        until not Settings.AutoFarmElite or v.Humanoid.Health <= 0 or not v.Parent
                    end
                end

                if not Workspace.Enemies:FindFirstChild("Diablo") and not Workspace.Enemies:FindFirstChild("Deandre") and not Workspace.Enemies:FindFirstChild("Urban") then
                    ReplicatedStorage.Remotes.CommF_:InvokeServer("EliteHunter")
                end
            end)
        end
    end)
end

-- Auto Stats Function
function AutoStats()
    task.spawn(function()
        while Settings.AutoStats do
            task.wait(1)
            pcall(function()
                if Settings.SelectedStats == "Melee" then
                    ReplicatedStorage.Remotes.CommF_:InvokeServer("AddPoint", "Melee", 1)
                elseif Settings.SelectedStats == "Defense" then
                    ReplicatedStorage.Remotes.CommF_:InvokeServer("AddPoint", "Defense", 1)
                elseif Settings.SelectedStats == "Sword" then
                    ReplicatedStorage.Remotes.CommF_:InvokeServer("AddPoint", "Sword", 1)
                elseif Settings.SelectedStats == "Gun" then
                    ReplicatedStorage.Remotes.CommF_:InvokeServer("AddPoint", "Gun", 1)
                elseif Settings.SelectedStats == "Blox Fruit" then
                    ReplicatedStorage.Remotes.CommF_:InvokeServer("AddPoint", "Demon Fruit", 1)
                end
            end)
        end
    end)
end

-- Main Loop for attacks
RunService.Heartbeat:Connect(function()
    pcall(function()
        if AttackFunction then
            AttackFunction()
        end
        AutoClick()
    end)
end)

-- Auto Character Respawn
Player.CharacterAdded:Connect(function(char)
    Character = char
    Humanoid = char:WaitForChild("Humanoid")
    RootPart = char:WaitForChild("HumanoidRootPart")

    if Settings.WalkSpeed ~= 16 then
        Humanoid.WalkSpeed = Settings.WalkSpeed
    end
    if Settings.JumpPower ~= 50 then
        Humanoid.JumpPower = Settings.JumpPower
    end
end)

-- Auto Rejoin
if Settings.AutoRejoin then
    game.CoreGui.RobloxPromptGui.promptOverlay.ChildAdded:Connect(function(child)
        if child.Name == 'ErrorPrompt' and child:FindFirstChild('MessageArea') and child.MessageArea:FindFirstChild('ErrorFrame') then
            game:GetService("TeleportService"):Teleport(game.PlaceId)
        end
    end)
end

-- Initialize Mobile UI
task.spawn(function()
    task.wait(2)
    CreateMobileUI()
end)

print("========================================")
print("🌟 NEON HUB V1 - KNRL MOBILE EDITION 🌟")
print("Created by XzgyX")
print("✅ Compatível com KNRL Mobile")
print("✅ UI otimizada para mobile")
print("✅ Proteção anti-detecção aprimorada")
print("✅ Todas as funções funcionando")
print("✅ Interface responsiva")
print("========================================")

-- Success notification
task.spawn(function()
    task.wait(3)
    pcall(function()
        StarterGui:SetCore("SendNotification", {
            Title = "🌟 Neon Hub V1",
            Text = "Carregado com sucesso no KNRL Mobile!",
            Duration = 5
        })
    end)
end)
